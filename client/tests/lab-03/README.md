# Lab 3 client test harness

The entry points below are intentionally `todo` until their corresponding
implementation issues land. Keeping the paths now makes Test DD traceability
visible without claiming that unimplemented UI behavior passes.

# Lab 3 E2E harness

Playwright is pinned in `e2e/package.json`. The current files are skipped
placeholders because authentication and the final application routes are
implemented in later issues. The CI job can install the browser and enumerate
these tests without claiming that the workflows pass.
